SELECT
  LENGTH(CAST(name as text)) as id, LENGTH(CAST(legs as text)) as name, LENGTH(CAST(arms as text)) as legs, LENGTH(CAST(characteristics as text)) as arms, LENGTH(CAST(id as text)) as characteristics
FROM monsters